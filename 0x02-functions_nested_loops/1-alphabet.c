#include "main.h"
/**
 * print_alphabet - prints the english alphabet from a-z.
 * Return: Always 0.
 */
void print_alphabet(void)
{
/* Declare char */
char c;
/* for loop condition */
for (c = 'a'; c <= 'z'; c++)
{
_putchar(c);
}
/* prints new line */
_putchar('\n');
}
